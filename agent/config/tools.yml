# 工具配置

# HTTP 请求配置
http:
  timeout: 30
  max_retries: 3

# 工具加载配置
loading:
  # 后端 API 端点
  api_endpoint: "/tools/actions"
  # 验证工具可用性
  validate_on_load: true

# 工具匹配配置
matching:
  # RAG 语义匹配阈值（低于此值不匹配）
  rag_threshold: 0.4
